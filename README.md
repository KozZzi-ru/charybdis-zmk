# Charybdis ZMK: Swap trackball layers

## Изменения 12.11.2025 - Устранение дублирования инверсий трекбола

### Проблема:
Бессистемные задержки при активации трекбола из-за дублирования инверсий осей X/Y на уровнях CONFIG и OVERLAY.

### Решение:

**Принцип:** Двойная инверсия = отмена инверсии (2× invert = no invert).  
Исправление: Убраны CONFIG инверсии + инвертирована логика OVERLAY для **сохранения текущего поведения**.

### Изменённые файлы:

#### 1. `config/charybdis_left.conf`

**Изменения:**
- ✖️ Закомментированы: `CONFIG_PMW3610_INVERT_X` и `CONFIG_PMW3610_INVERT_Y` (дублировались с OVERLAY)
- ✖️ Закомментирован: `CONFIG_PMW3610_SMART_ALGORITHM` (может вызывать нестабильность при BLE split)
- ➕ Добавлены power management параметры:
  - `CONFIG_PMW3610_RUN_DOWNSHIFT_TIME_MS=5000` (5 секунд до сна)
  - `CONFIG_PMW3610_REST1_SAMPLE_TIME_MS=50` (медленнее polling в rest режиме)

#### 2. `config/boards/shields/charybdis/charybdis_left.overlay`

**Изменения:**
- **Базовый слой:** Убраны инверсии X/Y (было 2×, теперь 0×)
- **Scroll слой:** Заменена инверсия `X` на `Y` (было 2× X + 1× Y, теперь 0× X + 1× Y)
- **Snipe слой:** Убраны инверсии X/Y (было 2×, теперь 0×)
- ⬆️ Увеличен timeout `zip_temp_layer`: 1000мс → 2000мс (стабильность переключения слоев)

### Поведение трекбола (сохранено):

| Слой | X ось | Y ось | Статус |
|---------|--------|--------|----------|
| **Базовый** | ⚪ Без инверсии | ⚪ Без инверсии | ✅ Сохранено |
| **Scroll** | ⚪ Без инверсии | ✅ Инвертирован | ✅ Сохранено |
| **Snipe** | ⚪ Без инверсии | ⚪ Без инверсии | ✅ Сохранено |

### Результат:
✅ **Дублирование устранено** → задержки активации устранены  
✅ **Текущее поведение сохранено**  
✅ **Per-layer гибкость сохранена**  
✅ **Стабильность улучшена**

### Технические детали:

**ДО исправления:**
```
CONFIG: INVERT_X=✅, INVERT_Y=✅
OVERLAY (Базовый): INVERT_X=✅, INVERT_Y=✅
ИТОГО: 2× инверсии = отменяют друг друга ❌
```

**ПОСЛЕ исправления:**
```
CONFIG: INVERT_X=❌, INVERT_Y=❌
OVERLAY (Базовый): INVERT_X=❌, INVERT_Y=❌
ИТОГО: 0× инверсий = без инверсии ✅
```

---

## Изменения 07.11.2025 - Переключение слоёв трекбола

### Что изменено:

**Активация трекбола:**
- Было: при движении трекбола → слой 2 (mouse)
- **Стало: при движении трекбола → слой 3 (scroll)**

**Переключение по кнопке:**
- Было: зажатие кнопки → слой 3 (scroll)
- **Стало: зажатие кнопки → слой 2 (mouse)**

### Изменённые файлы:

1. **`config/boards/shields/charybdis/charybdis_left.overlay`**
   - Изменено: `&zip_temp_layer 2 1000` → `&zip_temp_layer 3 1000`
   - Эффект: трекбол теперь активирует слой 3 (scroll) по умолчанию

2. **`config/charybdis.keymap`**
   - Изменено в слое `QWERTY` (layer 0): `&lt 3 RSHIFT` → `&lt 2 RSHIFT`
   - Эффект: при зажатии кнопки включается слой mouse

### Как это работает:

- При движении трекбола: автоматически включается **scroll** на 2 секунды
- Если зажать кнопку: включается **mouse** режим
- Слой 4 (sniper) остаётся доступен через слой 4 (mouse)

---

## Предыдущие изменения (fix-scroll-smooth)

- **Плавность скролла увеличена**: добавлен zip_scroll_scaler со множителем 2
- **Скорость скролла чуть выше**: zip_xy_scaler теперь 1 10
- **Режим "sniper" исправлен**: убрана инверсия axis transform
- **Оси в скролле**:
    - Горизонтальная: стандартная (нет инверсии)
    - Вертикальная: инвертирована (движение шара вниз = страница вниз)
- **Режим курсора**: глобальная инверсия X/Y

### Настройка плавности:
- zip_scroll_scaler 2 1 — плавнее. Можно попробовать 3 2 для более плавного эффекта
- zip_xy_scaler 1/2/3 10—15 — скорость прокрутки

---

**Ссылки:**
- [fix-trackball-inversion-duplication](https://github.com/KozZzi-ru/charybdis-zmk/tree/fix-trackball-inversion-duplication) - устранение дублирования
- [swap-trackball-layers](https://github.com/KozZzi-ru/charybdis-zmk/tree/swap-trackball-layers) - переключение слоёв
- [fix-scroll-smooth](https://github.com/KozZzi-ru/charybdis-zmk/tree/fix-scroll-smooth) - базовая ветка
